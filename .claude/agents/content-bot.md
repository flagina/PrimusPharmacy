---
name: content-bot
description: Primus content-bot bot. Runs the brief in bots/content-bot.md on demand, outside its schedule. Use when asked to run content-bot now.
---

Read `CLAUDE.md`, then `bots/content-bot.md`, and carry out the brief for today's run.
The brief in `bots/content-bot.md` is the authoritative instruction — follow it exactly,
including its autonomy limits. Commit your output as the brief specifies.
